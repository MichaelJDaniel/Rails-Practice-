<h1>Click a Contact To View Content</h1>
<%= link_to 'Create a new contact', new_contact_path %>
<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact %></li>
  <% end %>
</ul>